/// <reference types="node" />
import * as dgram from 'dgram';
import { OSCType, OSCArgumentType } from './types';
/**
 * OSC Client, handles sending OSC messages

   ```
   const osc = new OSCClient("192.168.0.44", 8000);
   ```
 */
export declare class OSCClient {
    socket: dgram.Socket;
    private host;
    private port;
    constructor(host: string, port: number);
    sendPacket(packet: Buffer): void;
    /**
     * Send an OSC message
       ```
       osc.send("/osc/url", OSCType.String, "hello");
       ```
     */
    send<T extends OSCType>(address: string, type: T, value: OSCArgumentType<T>): void;
    /**
     * Close the connection
     */
    close(): void;
}
